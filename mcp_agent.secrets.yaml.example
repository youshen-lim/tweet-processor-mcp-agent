# =============================================================================
# MCP AGENT SECRETS CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to mcp_agent.secrets.yaml and fill in your actual API keys
# NEVER commit mcp_agent.secrets.yaml to version control!
#
# This file follows the MCP Agent Cloud configuration schema
# See: https://docs.mcp-agent.com/cloud/overview

$schema: mcp-agent-main/schema/mcp-agent.config.schema.json

# =============================================================================
# ANTHROPIC API CONFIGURATION
# =============================================================================
# Get your API key from: https://console.anthropic.com/
# Required if using Claude models (Sonnet, Haiku, Opus)

anthropic:
  api_key: "sk-ant-api03-YOUR-ANTHROPIC-API-KEY-HERE"

# =============================================================================
# OPENAI API CONFIGURATION (Optional)
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# Required if using OpenAI models (GPT-4, GPT-4o, GPT-4o-mini)

# openai:
#   api_key: "sk-YOUR-OPENAI-API-KEY-HERE"

# =============================================================================
# NOTES
# =============================================================================
# 1. This file contains SENSITIVE API KEYS - never commit to version control
# 2. The .gitignore file should include: mcp_agent.secrets.yaml
# 3. Store this file securely and back it up in a safe location
# 4. Rotate API keys regularly for security
# 5. Use environment-specific secrets for development vs production

